@Library('my-shared-library') _

pipeline {
    agent any
    stages {
        stage('Example Stage') {
            steps {
                my.shared.MyCustomStep {
                    parameter = "Hello, world!"
                }
            }
        }
    }
}

def result = my.shared.calculateSum(5, 3)
echo "Sum: ${result}"
